name: hyun-deployment

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Deploy to HyunActions
        uses: Hyunsu-Playground/Hyun-Action@beta